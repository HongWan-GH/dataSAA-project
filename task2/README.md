# Task 2: Self‑Study on Graph and BFS/DFS

## Topic
- **Data Structure**: Graph (adjacency list representation)
- **Algorithms**: Breadth‑First Search (BFS) and Depth‑First Search (DFS) – both recursive and iterative implementations.

## Current Status (Pre‑submission)
- Implemented `Graph` class with `add_edge` and traversal methods.
- BFS returns the order of visited nodes.
- DFS implemented recursively and iteratively.
- Simple test included in the file.

## Next Steps
- Add more graph operations (remove vertex/edge, check connectivity).
- Analyze time/space complexity for the final report.
- Explore applications: shortest path (BFS), cycle detection (DFS).

## How to Run
Execute:
python graph_bfs.py


## File Structure
- `graph_bfs.py` – Contains Graph class and traversal algorithms.
